from textwrap import dedent


nodes = {
    'precise_small': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=1&labels=amd64+precise+small+x86_64&nodename=precise__%s" | bash
        """),
        'keyname': 'andrewschoen',
        'image_name': 'Ubuntu 12.04',
        'size': 'vps-ssd-1',
        'labels': ['amd64', 'x86_64', 'precise', 'small'],
        'provider': 'openstack'
    },
    'precise_huge': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=1&labels=amd64+precise+huge+x86_64&nodename=precise__%s" | bash
        """),
        'keyname': 'andrewschoen',
        'image_name': 'Ubuntu 12.04',
        'size': 'eg-30',
        'labels': ['amd64', 'x86_64', 'precise', 'huge'],
        'provider': 'openstack'
    },
    'wheezy_small': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=1&labels=amd64+wheezy+small+x86_64&nodename=wheezy__%s" | bash
        """),
        'keyname': 'andrewschoen',
        'image_name': 'Debian 7',
        'size': 'vps-ssd-1',
        'labels': ['amd64', 'x86_64', 'wheezy', 'small'],
        'provider': 'openstack'
    },
    'wheezy_huge': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=1&labels=amd64+wheezy+huge+x86_64&nodename=wheezy__%s" | bash
        """),
        'keyname': 'andrewschoen',
        'image_name': 'Debian 7',
        'size': 'eg-30',
        'labels': ['amd64', 'x86_64', 'wheezy', 'huge'],
        'provider': 'openstack'
    },
    'jessie_small': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=1&labels=amd64+jessie+small+x86_64&nodename=jessie__%s" | bash
        """),
        'keyname': 'andrewschoen',
        'image_name': 'Debian 8',
        'size': 'vps-ssd-1',
        'labels': ['amd64', 'x86_64', 'jessie', 'small'],
        'provider': 'openstack'
    },
    'jessie_huge': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=1&labels=amd64+wheezy+huge+x86_64&nodename=jessie__%s" | bash
        """),
        'keyname': 'andrewschoen',
        'image_name': 'Debian 8',
        'size': 'eg-30',
        'labels': ['amd64', 'x86_64', 'jessie', 'huge'],
        'provider': 'openstack'
    },
    'trusty_small': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=1&labels=amd64+trusty+small+x86_64&nodename=trusty__%s" | bash
        """),
        'keyname': 'andrewschoen',
        'image_name': 'Ubuntu 14.04',
        'size': 'vps-ssd-1',
        'labels': ['amd64', 'x86_64', 'trusty', 'small'],
        'provider': 'openstack'
    },
    'trusty_huge': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=1&labels=amd64+trusty+huge+x86_64&nodename=trusty__%s" | bash
        """),
        'keyname': 'andrewschoen',
        'image_name': 'Ubuntu 14.04',
        'size': 'eg-30',
        'labels': ['amd64', 'x86_64', 'trusty', 'huge'],
        'provider': 'openstack'
    },
    'centos6_small': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=2&labels=amd64+centos6+x86_64+small&nodename=centos6__%s" | bash"""),
        'keyname': 'andrewschoen',
        'image_name': 'Centos 6',
        'size': 'vps-ssd-1',
        'labels': ['amd64', 'x86_64', 'centos6', 'small'],
        'provider': 'openstack'
    },
    'centos6_huge': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=2&labels=amd64+centos6+x86_64+huge&nodename=centos6__%s" | bash"""),
        'keyname': 'andrewschoen',
        'image_name': 'Centos 6',
        'size': 'eg-30',
        'labels': ['amd64', 'x86_64', 'centos6', 'huge'],
        'provider': 'openstack'
    },
    'centos7_small': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=2&labels=amd64+centos7+small+x86_64&nodename=centos7__%s" | bash"""),
        'keyname': 'andrewschoen',
        'image_name': 'Centos 7',
        'size': 'vps-ssd-1',
        'labels': ['amd64', 'x86_64', 'centos7', 'small'],
        'provider': 'openstack'
    },
    'centos7_huge': {
        'script': dedent("""#!/bin/bash
        curl -L "{{ prado_url }}/setup/slave/?token={{ jenkins_prado_token }}&executors=2&labels=amd64+centos7+huge+x86_64&nodename=centos7__%s" | bash"""),
        'keyname': 'andrewschoen',
        'image_name': 'Centos 7',
        'size': 'eg-30',
        'labels': ['amd64', 'x86_64', 'centos7', 'huge'],
        'provider': 'openstack'
    },
    '__force_dict__': True

}
